channels:
  - conda-forge
  - usgs-astrogeology

dependencies:
  - cmake
  - cpp-filesystem
  - cxx-compiler
  - doxygen
  - ninja
  - pip
  - pytest
  - python
  - swig
  - numpy
  # Native libraries required by a default-options build (find_package REQUIRED):
  - zlib
  - lz4-c
  - libcurl
  - openssl
  - pip:
    - mkdocs
    - mkdoxy
    - mkdocs-material
